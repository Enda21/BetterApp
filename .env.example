# Example environment variables for BetterApp
# Do NOT commit real secrets. Copy to .env and fill in locally if needed.

# If you ever need Typeform IDs (non-secret), keep them here.
TYPEFORM_FORM_ID=

# Do NOT put Typeform Personal Access Tokens in the mobile app.
# Use a backend service to call Typeform APIs securely.
# TYPEFORM_API_TOKEN=
